name: Security

on:
  pull_request:
    branches: [main, v3.x]
  workflow_dispatch:

jobs:
  dependency-review:
    uses: PayPal-Braintree/security-workflows/.github/workflows/dependency-review.yml@main
