name: Security

on:
  pull_request:
    branches: [master, v3.x]
  workflow_dispatch:

jobs:
  dependency-review:
    uses: PayPal-Braintree/security-workflows/.github/workflows/dependency-review.yml@main

  CodeScanning-Actions:
    uses: PayPal-Braintree/security-workflows/.github/workflows/codeql-actions.yml@feature/enable-github-actions-scanning
